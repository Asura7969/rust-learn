name: Rust CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Rust
        uses: actions/checkout@v3
      - name: Protoc
        uses: apt-get install protobuf-compiler  
      - name: Install cargo-audit
        run: cargo install cargo-audit
      - name: Check code format
        run: cargo fmt -- --check  
      - name: Build
        run: cargo build --verbose
      - name: Test
        run: cargo test --verbose
      - name: Clippy
        run: cargo clippy --verbose -- -D warnings
      - name: Audit
        run: cargo audit